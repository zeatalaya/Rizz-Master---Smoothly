services:
  rizz-master:
    image: ghcr.io/zeatalaya/rizz-master-smoothly:phala
    ports:
      - "3069:3069"
    environment:
      - NODE_ENV=production
      - SESSION_SECRET=TEE-rizz-master-smoothly-32char-secret-key!!
    volumes:
      - /var/run/dstack.sock:/var/run/dstack.sock
